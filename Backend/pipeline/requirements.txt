# =============================================================
# AI Video Inpainting — Python Dependencies
# =============================================================
#
# IMPORTANT: Install PyTorch with CUDA FIRST (before this file):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Then install the rest:
#   pip install -r requirements.txt
#
# Also required (installed separately):
#   - FFmpeg: https://ffmpeg.org/download.html (add to system PATH)
#   - Ollama: https://ollama.com/download (then run: ollama pull llava)
# =============================================================

# Video processing
opencv-python>=4.8.0
moviepy>=2.0.0

# AI model inference (Stable Video Diffusion)
diffusers>=0.25.0
transformers>=4.36.0
accelerate>=0.25.0
safetensors>=0.4.0

# Local LLM (Ollama client)
ollama>=0.4.0

# Configuration
python-dotenv>=1.0.0

# Image processing
Pillow>=10.0.0
